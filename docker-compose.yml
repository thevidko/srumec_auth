# Verze je zastaralá, proto ji vynecháváme

services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: srumec_auth
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  api:
    build: .
    command: ["fastapi", "dev", "main.py", "--host", "0.0.0.0"]

    # Změna zde: připojujeme lokální složku do /code
    volumes:
      - .:/code

    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db

    # Změna zde: cesta k entrypointu je teď v /code
    entrypoint: /code/entrypoint.sh

volumes:
  postgres_data: